var id = <%= @answer.id %>
$('b.accepted').hide();
$('.accept').show();
$('li#answer-' + id).prependTo('ul.list-group');
$('b').html('');
$('<b> Accepted <b>').prependTo( $('li#answer-' + id) );
$('#accept-' + id).hide();