var id = <%= @answer.id %>
$('b.accepted').hide();
$('.accept').show();
$('li#answer-row-' + id).prependTo('ul.list-group');
$('b').html('');
$('<b> Accepted <b>').prependTo( $('li#answer-row-' + id) );
$('#accept-' + id).hide();